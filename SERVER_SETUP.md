# Laravel Server Configuration

## 8085 portda serverini ishga tushirish

Serverni ishga tushirish uchun quyidagi buyruqni bajaring:

```bash
./start-server.sh
```

Yoki to'g'ridan-to'g'ri:

```bash
php artisan serve --host=0.0.0.0 --port=8085
```

Server manzili: http://localhost:8085
